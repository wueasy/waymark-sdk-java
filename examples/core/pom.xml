<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wueasy.waymark</groupId>
    <artifactId>waymark-java-example-core</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>waymark-java-example-core</name>
    <description>Waymark Java SDK core 示例工程（JDK 1.8，快速初始化：配置监听 + 服务发现）</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- 与核心 SDK 保持一致，示例同样兼容 JDK 1.8 -->
        <maven.compiler.release>8</maven.compiler.release>
        <waymark.version>0.1.0</waymark.version>
        <slf4j.version>1.7.36</slf4j.version>
        <logback.version>1.2.12</logback.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.wueasy.waymark</groupId>
            <artifactId>waymark-java</artifactId>
            <version>${waymark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <!-- 示例运行时的日志实现：SDK 调试日志与 -log 开关依赖它输出 -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
            </plugin>
            <!-- 用 mvn exec:java -Dexec.mainClass=... 运行各示例入口 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.4.0</version>
            </plugin>
        </plugins>
    </build>
</project>
