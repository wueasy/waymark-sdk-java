<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wueasy.waymark</groupId>
    <artifactId>waymark-java-parent</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <name>waymark-java-parent</name>
    <description>Waymark Java SDK 父工程：配置中心、注册中心与服务发现</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- 核心 SDK 兼容 JDK 1.8 -->
        <maven.compiler.release>8</maven.compiler.release>
        <jackson.version>2.15.4</jackson.version>
        <slf4j.version>1.7.36</slf4j.version>
        <!-- Spring Boot 2 分支版本（JDK 8） -->
        <spring-boot2.version>2.7.12</spring-boot2.version>
        <!-- Spring Boot 3 分支版本（JDK 17） -->
        <spring-boot3.version>3.3.6</spring-boot3.version>
    </properties>

    <modules>
        <module>waymark-java</module>
        <module>waymark-spring-boot2-starter</module>
        <module>waymark-spring-boot3-starter</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.wueasy.waymark</groupId>
                <artifactId>waymark-java</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.3.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.1.2</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
